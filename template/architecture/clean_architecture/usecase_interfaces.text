package {{.Meta.service}}_usecase

import (
    "{{.ModuleName}}/{{.ModelPath}}"
    "context"
    "{{.ModuleName}}/{{.ResultPath}}/{{.Meta.service}}/{{.Meta.service}}_request"
)

type {{.Model}}Usecase interface {
	Store(ctx context.Context, req {{.Meta.service}}_request.{{.Model}}Store) (r model.{{.Model}}, err error)
	Update(ctx context.Context, req {{.Meta.service}}_request.{{.Model}}Store) (r model.{{.Model}}, err error)
}
